# Google Control Plane Boundary

`HTTP 200 != indexed`

`robots allowed != indexed`

`sitemap member != indexed`

`Search Console submitted != guaranteed ranking`

The repository owns technical crawl/index readiness. Google owns crawl scheduling, indexing and ranking. Completion therefore requires both a green site-side receipt and a separate Search Console receipt.
